_base_: D:/Github-Projects/SkillOpt/configs/_base_/default.yaml
model:
  optimizer: gpt-5.4
  target: gpt-5.4
  reasoning_effort: medium
  backend: azure_openai
train:
  train_size: 0
  batch_size: 30
  accumulation: 1
  num_epochs: 4
  seed: 42
gradient:
  minibatch_size: 4
  merge_batch_size: 4
  analyst_workers: 4
  failure_only: true
optimizer:
  learning_rate: 4
  skill_update_mode: patch
  lr_scheduler: cosine
  use_slow_update: true
  use_meta_skill: true
evaluation:
  use_gate: true
  sel_env_num: 100
  test_env_num: 113
env:
  name: generic_csv
  skill_init: train/ceramic_capacitor_initial.md
  split_mode: ratio
  split_ratio: '6:2:2'
  split_seed: 42
  data_path: train/ceramic_capacitor.json
  max_turns: 1
  max_completion_tokens: 4096
  workers: 20
